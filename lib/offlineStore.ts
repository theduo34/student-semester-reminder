// AsyncStorage-backed offline cache, mutation outbox, and the client-derived alerts
// read-state (see AGENTS.md "Alerts feed" section) will live here — built feature by
// feature, not scaffolded yet.
