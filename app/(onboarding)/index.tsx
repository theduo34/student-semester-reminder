import { View } from 'react-native';

// Shown when the student is authenticated but no semester has been published yet.
export default function OnboardingScreen() {
  return <View className="flex-1 bg-background" />;
}
