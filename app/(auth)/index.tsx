import { View } from 'react-native';

// Login / register tabs, backed by the Convex Auth Password provider.
// No admin sign-in link here — the Academic Admin app is a separate app entirely.
export default function AuthScreen() {
  return <View className="flex-1 bg-background" />;
}
